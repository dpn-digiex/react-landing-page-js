import styles from "./index.module.scss";

const ProjectPage = () => {
  return (
    <div id="project-page" className={styles.pageContainer}>
      ProjectPage
    </div>
  );
};

export default ProjectPage;
